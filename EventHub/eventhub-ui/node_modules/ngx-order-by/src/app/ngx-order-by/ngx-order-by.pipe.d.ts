import { PipeTransform } from '@angular/core';
export declare class NgxOrderByPipe implements PipeTransform {
    private fields;
    private reverse;
    transform(values: Array<Object>, fields: Array<string>, reverse: boolean, changeIndicator?: number): any;
    private compare(a, b);
    private dynamicSort(a, b, property);
}
